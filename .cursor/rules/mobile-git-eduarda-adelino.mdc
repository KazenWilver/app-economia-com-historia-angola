---
description: Eduarda e Adelino só para commits mobile; resto Willfredy/Sulo
globs: mobile/**/*
alwaysApply: false
---

# Git — Mobile apenas (Eduarda & Adelino)

## Âmbito (obrigatório)

- **Eduarda** e **Adelino** → **somente** commits que tocam `mobile/` (e fixes mínimos directamente ligados ao Expo, ex. CORS 8081/8082).
- **Nunca** usar Eduarda/Adelino para backend, frontend web, shared genérico, Docker, CI, docs gerais, etc.
- Fora do mobile: continuar com **Willfredy** / **Sulo** (ver `divisao-equipa.mdc`).

## Fluxo no mobile

Ao concluir trabalho em `mobile/`:

1. **Sempre** commit(s) → `.\scripts\pre-push.ps1` → `git push` (sem esperar pedido).
2. Autores (autor = committer; preferir env vars):
   - **Adelino** `<adelinodo003@gmail.com>`
   - **Eduarda** `<Adraude2694@gmail.com>`
3. Alternar entre os dois quando houver mais do que um commit lógico.
4. Ou: `. .\scripts\git-profile.ps1` → `Commit-Git -Who adelino|eduarda -Message "..."`.
